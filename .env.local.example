# Supabase (복사 후 .env.local 로 저장하고 실제 값으로 채우세요)
# .env.local 은 git에 올리지 마세요.

NEXT_PUBLIC_SUPABASE_URL=https://xxxxxxxxxxxx.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.xxxxx...

# 로그인 사용: Supabase 대시보드 → Authentication → URL Configuration 에서
# Site URL = https://your-domain.com (또는 로컬이면 http://localhost:3000)
# Redirect URLs 에 https://your-domain.com/auth/callback, http://localhost:3000/auth/callback 추가

# 파비콘(선택): 이미지 URL을 넣으면 탭·앱 아이콘으로 사용
# NEXT_PUBLIC_FAVICON_URL=https://example.com/my-icon.png
